<section class="emblems pt-{{ section.settings.pt }} pb-{{ section.settings.pb }}" style="background: {{ section.settings.background }}">
  <div class="container-fluid">
    {% if section.settings.title.size > 0 %}
      <div class="text-center text-uppercase fw-semibold mb-3">
        {{ section.settings.title }}
      </div>
    {% endif %}
  </div>
  <div class="overflow-scroll">
    <div class="d-flex justify-content-around align-items-center gap-5">
      {%- for block in section.blocks -%}
        <div class="col-4 col-md-{{ section.settings.size }}">
          {% if block.settings.image %}
            {{ block.settings.image | img_tag: "", "w-100 h-auto", 'large' }}
          {% else %}
            {{ 'image' | placeholder_svg_tag: 'w-100 h-auto' }}
          {% endif %}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>


{%- schema -%}
  {
    "name": "emblems",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Emblems Title"
      },
      {
        "type": "range",
        "id": "pt",
        "default": 3,
        "min": 0,
        "max": 5,
        "label": "Padding Top"
      },
      {
        "type": "range",
        "id": "pb",
        "default": 3,
        "min": 0,
        "max": 5,
        "label": "Padding Bottom"
      },
      {
        "type": "color_background",
        "id": "background",
        "label": "Background",
        "default": "linear-gradient(180deg, #fff, #fff)"
      }, {
        "type": "range",
        "id": "size",
        "default": 2,
        "min": 1,
        "max": 6,
        "step": 1,
        "label": "Emblem Size"
      }
    ],
    "blocks": [
      {
        "name": "Emblem",
        "type": "emblem",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          }
        ]
      }
    ],
    "presets": [
      {
        "category": "Content",
        "name": "Emblems Section",
        "blocks": [
          {
            "type": "emblem"
          }, {
            "type": "emblem"
          }, {
            "type": "emblem"
          }
        ]
      }
    ]
  }
{%- endschema -%}