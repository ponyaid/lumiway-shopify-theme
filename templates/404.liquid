<section class="py-5 bg-light">
  <div class="container">
    <div class="text-center mt-2 mb-0 my-md-5">
      <h2 class="h1 fw-bold">Ooops..<br/>This page doesn't exist</h2>
      <p class="h6 mb-4">{{ 'general.404.subtext_html' | t }}</p>
      <a href="{{ routes.root_url }}" class="btn btn-primary">Go to home page</a>
    </div>
  </div>
</section>

{% section 'list-collections' %}